---
name: PR Review Command

on:
  issue_comment:
    types: [created]

jobs:
  review:
    if: github.event.issue.pull_request && contains(github.event.comment.body, '/review')
    runs-on: ubuntu-latest
    steps:
      - name: Review command received
        run: |
          echo "============================================"
          echo " Review command triggered"
          echo " Triggered by: @${{ github.event.comment.user.login }}"
          echo " PR: #${{ github.event.issue.number }}"
          echo " Comment URL: ${{ github.event.comment.html_url }}"
          echo "============================================"

      # TODO: 在这里添加后续实际的 CI 任务
